const {setMode} = require('./setMode')
const {setEntry} = require('./setEntry')
const {setOutput} = require('./setOutput')
const {setModule} = require('./setModule')
const {setResolve} = require('./setResolve')
const {setPlugins} = require('./setPlugins')
const {setDevtool} = require('./setDevtool')
const {setExternals} = require('./setExternals')
const {setDevServer} = require('./setDevServer')

module.exports = {
  setMode,
  setEntry,
  setOutput,
  setModule,
  setResolve,
  setPlugins,
  setDevtool,
  setExternals,
  setDevServer,
}
